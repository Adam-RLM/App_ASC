<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:windowTranslucentNavigation">true</item>-->
    </style>

    <style name="toasttyle_entrada">
        <item name="textSize">25sp</item>
        <item name="colorBackground">#94ffab</item>
        <item name="textColor">#000</item>
        <item name="iconLeft">@drawable/ic_timer_marca_entrada</item>
        <item name="strokeColor">#000</item>
        <item name="strokeWidth">3sp</item>
        <item name="length">LONG</item>
        <item name="cornerRadius">8sp</item>
    </style>

    <style name="toasttyle_salida">
        <item name="textSize">25sp</item>
        <item name="colorBackground">#FF0000</item>
        <item name="textColor">#FFFFFF</item>
        <item name="iconLeft">@drawable/ic_timer_marca_salida</item>
        <item name="strokeColor">#FFFFFF</item>
        <item name="strokeWidth">3sp</item>
        <item name="length">LONG</item>
        <item name="cornerRadius">8sp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

</resources>
